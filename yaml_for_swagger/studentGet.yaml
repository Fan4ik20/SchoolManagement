Returns a information about invidividual student in JSON format.
---
parameters:
 - in: path
   name: student_id
   type: integer
   required: true

tags:
 - Students

responses:
 200:
   description: Returns a information about student in JSON format.
 404:
   description: Occurs if student with passed id does not exists.


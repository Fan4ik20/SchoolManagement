Returns a information about invidividual group in JSON format.
---
parameters:
 - in: path
   name: group_id
   type: integer
   required: true

tags:
 - Groups

responses:
 200:
   description: Returns a information about group in JSON format.
 404:
   description: Occurs if group with passed id does not exists.

